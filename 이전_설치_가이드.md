# VideoMaker (YouTube PD) — 다른 PC 이전·설치 가이드

> 이 문서는 VideoMaker 프로젝트를 **새 PC(로컬 환경)**에서 처음부터 구동하기 위한 체크리스트다.
> (서버/클라우드 배포는 `EC2_DEPLOYMENT_GUIDE.md`를 참고. 이 문서는 개인 PC 이전용이다.)

VideoMaker는 단순 웹앱이 아니라 **시스템 도구(ffmpeg)·외부 CLI(claude)·로컬 앱(CapCut)** 에 의존한다. 코드만 복사하면 안 되고 아래 순서를 따라야 한다.

---

## 0. 한눈에 보기 (요약 체크리스트)

- [ ] Node.js(LTS 20+) 설치
- [ ] ffmpeg / ffprobe 설치 + PATH 등록
- [ ] CapCut 데스크톱 앱 설치
- [ ] Claude 인증 준비 (CLI 로그인 **또는** API 키)
- [ ] 소스 코드 복사 (`node_modules` / `.next` 제외)
- [ ] `data/` 폴더 복사 (기존 프로젝트를 이어서 쓸 경우만)
- [ ] `.env.local` 새로 작성
- [ ] `npm install` (sharp 네이티브 모듈 재빌드)
- [ ] `npm run dev` → http://localhost:3000 접속 확인
- [ ] 앱 설정 화면에서 CapCut 경로 확인/지정

---

## 1. 필수 소프트웨어 설치

### Node.js
- Next.js 16 구동에 필요. **LTS 최신(v20 이상)** 권장.
- 설치: https://nodejs.org 또는 nvm/Homebrew.
- 확인: `node -v`, `npm -v`

### ffmpeg / ffprobe (필수)
- CapCut 빌드 단계(`lib/pipeline/capcut-editor.ts`)가 `execSync`로 직접 호출한다. 없으면 영상 길이·해상도 조회와 마지막 프레임 추출이 실패한다.
- macOS: `brew install ffmpeg`
- Windows: ffmpeg 다운로드 후 `bin` 폴더를 시스템 PATH에 등록
- Linux: `sudo apt install ffmpeg`
- 확인: `ffmpeg -version`, `ffprobe -version`

### CapCut 데스크톱 앱
- 최종 산출물이 CapCut 드래프트 폴더에 직접 기록된다. 설치돼 있어야 결과물이 CapCut 프로젝트 목록에 나타난다.

---

## 2. Claude 인증 — 둘 중 하나 선택

파이프라인의 모든 텍스트 생성은 Claude로 이뤄진다. 다음 두 모드 중 하나를 준비한다.

### 방법 A — claude CLI 모드
1. 새 PC에 `claude` CLI 설치 후 로그인(구독 인증).
2. `.env.local`의 `CLAUDE_BIN`에 바이너리 경로 지정.
   - 경로 확인: macOS/Linux `which claude`, Windows `where claude`
   - (최적화 작업 4가 적용돼 있으면 `CLAUDE_BIN` 미설정 시 자동으로 `which claude` 폴백을 시도한다.)

### 방법 B — Anthropic API 키 모드
1. `.env.local`에 `ANTHROPIC_API_KEY`만 설정.
2. 토큰 비용이 정확히 추적되고 프롬프트 캐싱이 동작한다(권장).

---

## 3. 소스 코드 옮기기

`.gitignore`가 `/node_modules`, `/.next`, `/data`, `.env*`를 제외한다. git clone 또는 폴더 복사로 가져오되:

- **포함**: `app/`, `lib/`, `public/`, `docs/`, 각종 설정 파일(`package.json`, `tsconfig.json`, `next.config.ts` 등)
- **제외(복사 금지)**: `node_modules`(플랫폼별 재설치 필요), `.next`(빌드 캐시)

> `node_modules`를 그대로 복사하면 안 된다. `sharp`(이미지 텍스트 합성)는 OS·CPU 아키텍처별 네이티브 바이너리를 쓰는데, 현재 lock 파일은 macOS(darwin-arm64)용이다. 다른 환경에서는 반드시 `npm install`을 새로 돌려야 한다.

---

## 4. 기존 프로젝트 데이터 옮기기 (선택)

새로 시작하면 불필요하다. 기존 작업물을 이어서 쓰려면:

- `data/` 폴더 전체를 새 PC의 같은 위치로 복사한다.
  - `data/projects/{슬러그}/` — 프로젝트별 산출물(대본·오디오·이미지·영상·CapCut JSON 등)
  - `data/settings.json` — CapCut 경로·보이스 설정
  - `data/cost-log.json` — 비용 로그
- (S3 백업을 쓰는 경우) `S3_BUCKET`을 설정하면 로컬에 없는 프로젝트는 S3에서 자동 복원된다.

---

## 5. `.env.local` 작성

루트에 `.env.local`을 새로 만든다. `.env.local.example`을 복사해서 채우면 된다. **모든 키는 선택적**이며, 없는 키에 해당하는 단계는 자동으로 건너뛴다.

```env
# AI 인증 — 방법 A(CLI) 또는 방법 B(API 키) 중 하나
ANTHROPIC_API_KEY=          # 방법 B 사용 시
CLAUDE_BIN=                 # 방법 A 사용 시 claude 바이너리 경로

# 리서치 웹 검색 (선택, 무료 월 1000회) — https://tavily.com
TAVILY_API_KEY=

# TTS 음성 생성 (선택) — https://elevenlabs.io
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=        # 사용할 보이스 ID(앱 설정에서도 선택 가능)

# 이미지·영상 생성 (선택) — https://fal.ai
FAL_API_KEY=

# 영상 생성 폴백 (선택)
KLING_API_KEY=

# 산출물 원격 백업 (선택)
S3_BUCKET=
AWS_REGION=ap-northeast-2
```

| 단계 | 필요한 키 | 미설정 시 |
|------|-----------|-----------|
| 리서치 웹 검색 | `TAVILY_API_KEY` | 검색 생략, LLM 지식만 사용 |
| TTS 음성/자막 | `ELEVENLABS_API_KEY` | TTS 단계 건너뜀 |
| 이미지 생성 | `FAL_API_KEY` | 이미지 단계 건너뜀 |
| 영상 생성 | `FAL_API_KEY` (폴백 `KLING_API_KEY`) | 영상 단계 건너뜀 |
| 원격 백업 | `S3_BUCKET`, `AWS_REGION` | 로컬 파일만 사용 |

---

## 6. 설치 및 실행

```bash
# 프로젝트 폴더에서
npm install        # sharp 등 네이티브 모듈을 현재 플랫폼용으로 빌드
npm run dev        # 개발 서버 시작
```

브라우저에서 http://localhost:3000 접속.

타입 체크/빌드로 정상 여부 확인:
```bash
npx tsc --noEmit
npm run build
```

---

## 7. CapCut 드래프트 경로 확인

`lib/settings.ts`가 OS별 기본 경로를 자동 추정한다:

- **Windows**: `%LOCALAPPDATA%\CapCut\User Data\Projects\com.lveditor.draft`
- **macOS**: `~/Movies/CapCut/User Data/Projects/com.lveditor.draft`

경로가 다르면 앱 홈 화면의 설정에서 직접 지정한다(`data/settings.json`에 저장됨). 완성된 프로젝트는 **CapCut을 재시작하면** 목록에 자동으로 나타난다.

---

## 8. 자주 막히는 지점 (트러블슈팅)

- **`sharp` 설치 오류 / 이미지 합성 실패** → `node_modules`를 지우고 `npm install` 재실행(플랫폼 바이너리 재빌드).
- **CapCut 단계에서 ffprobe/ffmpeg 오류** → ffmpeg가 PATH에 없는 경우. `ffmpeg -version`으로 확인.
- **AI 단계가 즉시 실패** → CLI 모드인데 `claude` 로그인이 안 됐거나 `CLAUDE_BIN` 경로가 틀림. (또는 API 키 미설정)
- **start.sh가 동작 안 함** → 스크립트 안의 `APP_DIR=/Users/hongss/VideoMaker` 경로가 하드코딩돼 있다. 새 PC의 실제 경로로 수정할 것.
- **포트 충돌** → 3000 포트를 다른 앱이 쓰는 경우. 기존 프로세스를 종료하거나 포트를 변경.

---

## 부록 — 최소 구성으로 빠르게 띄우기

이미지·영상·TTS 없이 텍스트 파이프라인(리서치~대본)만 먼저 확인하려면:

1. Node.js만 설치
2. Claude 인증(방법 A 또는 B)만 준비
3. `npm install && npm run dev`
4. 주제 입력 → 리서치·전략·기획·대본·검수 단계까지 동작 확인

이후 ffmpeg·CapCut·각 API 키를 추가하며 단계를 하나씩 활성화하면 된다.
