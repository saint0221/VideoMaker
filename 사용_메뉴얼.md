---
title: VideoMaker (YouTube PD) 사용 메뉴얼
type: 운영자 사용 가이드
audience: 영상 제작 운영자
---

# VideoMaker (YouTube PD) 사용 메뉴얼

> 주제 한 줄을 입력하면 리서치부터 CapCut 편집 프로젝트 생성까지 자동으로 진행되는 영상 제작 도구의 **운영자용 사용법**이다.
> 설치/환경 구성은 `이전_설치_가이드.md`, 내부 구조는 `VideoMaker_프로젝트_분석.md`를 참고.

---

## 0. 필수 프로그램 목록

서버를 켜기 전에 아래 프로그램이 PC에 설치되어 있어야 한다. (상세 설치법은 `이전_설치_가이드.md` 참고)

| 프로그램 | 용도 | 필수 여부 | 비고 |
|----------|------|-----------|------|
| **Node.js** (LTS 20+) | 앱 구동(서버 실행) | 필수 | https://nodejs.org · 확인: `node -v` |
| **ffmpeg / ffprobe** | 영상 길이·해상도 조회, CapCut 빌드 | 필수 | mac `brew install ffmpeg` · Windows는 설치 후 PATH 등록 |
| **CapCut(캡컷) 데스크톱 앱** | 최종 편집 프로젝트 열기 | 필수 | 완성본이 CapCut 드래프트 폴더에 저장됨 |
| **Claude 인증 수단** | 모든 AI(텍스트) 단계 | 필수 | `claude` CLI 로그인 **또는** `ANTHROPIC_API_KEY` 중 하나 |
| 최신 웹 브라우저 | 앱 화면 접속 | 필수 | Chrome/Edge/Safari 등 (http://localhost:3000) |

### API 키 (선택 — 없으면 해당 단계만 자동으로 건너뜀)

| 키 | 활성화되는 기능 | 발급처 |
|----|----------------|--------|
| `TAVILY_API_KEY` | 리서치 웹 검색 | https://tavily.com (무료 월 1000회) |
| `ELEVENLABS_API_KEY` | TTS 음성·자막 생성 | https://elevenlabs.io |
| `FAL_API_KEY` | 이미지·영상 생성 | https://fal.ai |
| `KLING_API_KEY` | 영상 생성 폴백 | (fal.ai 경유) |
| `S3_BUCKET` / `AWS_REGION` | 산출물 원격 백업 | AWS |

> 키는 프로젝트 루트의 `.env.local` 파일에 넣는다. 필수 프로그램(Node·ffmpeg·CapCut·Claude 인증)만 갖추면 텍스트 파이프라인(리서치~대본)은 바로 동작하고, 음성·이미지·영상은 해당 키를 추가하는 순간 활성화된다.

> **Windows 주의**: ① `CLAUDE_BIN`에 claude 실행파일 전체 경로를 지정할 것, ② ffmpeg가 기본 미설치이므로 PATH 등록 필수.

---

## 1. 시작하기

### 1.1 서버 실행 & 접속
1. 프로젝트 폴더에서 서버를 켠다.
   - macOS: `./start.sh` (또는 `npm run dev`)
   - Windows: `npm run dev`
2. 브라우저에서 **http://localhost:3000** 접속.

### 1.2 최초 1회 설정 (홈 화면)
영상을 만들기 전에 두 가지를 확인한다.

- **CapCut 저장 경로**: 완성 프로젝트가 저장될 CapCut 드래프트 폴더. 자동 추정되지만, 다르면 "폴더 선택"으로 직접 지정한다. (한 번 지정하면 저장됨)
- **보이스(음성)**: TTS에 사용할 ElevenLabs 보이스를 선택한다. (ElevenLabs 키가 설정된 경우)

---

## 2. 전체 흐름 한눈에

```
주제 입력
  → ① 리서치
  → ② 유튜브 채널 분석        ◆ (URL 입력 — 생략 가능)
  → ③ 컨셉 전략              ◆ 컨셉 3안 중 택1
  → ④ 기획서
  → ⑤ 대본
  → ⑥ 팩트 체크
  → ⑦ 대본 검수 (+ 자동 수정)  ◆ (80점 미만 재수정 시에만 승인)
  → ⑧ TTS 음성 + 자막
  → ⑨ 씬 설계
  → ⑩ 이미지 프롬프트
       ◆ 이미지 생성 비용 확인
       ◆ (레퍼런스 이미지 업로드 — 선택)
  → ⑪ 이미지 생성             ◆ 샘플 확인 → 전체 → 결과 확인
  → ⑫ 영상 클립 생성
  → ⑬ CapCut 프로젝트 생성 → 완료
```

`◆` 표시가 **운영자의 확인·선택이 필요한 지점**(게이트)이다. 나머지는 자동 진행되며, 진행 로그는 화면에 실시간으로 흐른다.

---

## 3. 새 프로젝트 만들기

1. 홈 화면 상단에 **영상 주제**를 입력한다.
   - 예: `노시보 효과 1분 영상`, `조선시대 왕들의 충격적인 죽음`
   - 팁: 주제에 **"1분 / 30초 / 2분"** 같은 길이를 넣으면 씬 수와 대본 분량이 자동으로 맞춰진다.
2. **화면 비율**을 고른다: `16:9`(가로, 일반 영상) 또는 `9:16`(세로, 쇼츠).
3. **언어**를 고른다: 한국어(`ko`) 또는 영어(`en`).
4. 생성하면 프로젝트가 만들어지고 상세 화면으로 이동한다. **"시작"**을 누르면 파이프라인이 돌기 시작한다.

> 프로젝트는 주제명을 딴 폴더로 저장되며, 언제든 홈 목록에서 다시 열 수 있다.

---

## 3.5 이미지 모델 · LoRA · 스타일 설정

이미지가 만들어지기 전에, 상세 화면 상단의 **"이미지 모델"** 막대에서 어떤 모델로 그릴지와 스타일을 미리 정한다. 언제든 바꿀 수 있고, 바꾸면 자동 저장된다.

### 이미지 모델 (6종 중 택1)

| 모델 | LoRA 지원 | 대략 단가/장 | 특징 |
|------|:--------:|:-----------:|------|
| **FLUX.1 LoRA** | ✓ | ~$0.035 | 고품질 FLUX + 커스텀 스타일(LoRA). 화풍을 직접 학습시켜 쓰고 싶을 때 |
| **FLUX.1 schnell** | ✕ | ~$0.003 | 가장 빠르고 저렴. 초안·대량 생성·테스트용 |
| **fast-SDXL** | ✓ | ~$0.0025 | 최저가. SDXL 기반, LoRA 지원. 비용 최우선일 때 |
| **FLUX.2 LoRA** | ✓ | ~$0.021 | 신형 FLUX.2 + LoRA. 품질·비용 균형 + 커스텀 스타일 |
| **FLUX.1 dev** | ✕ | ~$0.025 | 고품질 표준 모델. 일반적인 고퀄 결과 |
| **FLUX.2** | ✕ | ~$0.012 | 신형 표준 모델. 품질 대비 가성비 |

선택 기준 요약:
- **비용을 아끼고 싶다** → fast-SDXL 또는 FLUX.1 schnell
- **품질 우선** → FLUX.1 dev 또는 FLUX.2
- **나만의 화풍·캐릭터를 일관되게** → LoRA 지원 모델(FLUX.1 LoRA / FLUX.2 LoRA / fast-SDXL)

각 모델 옆에 **`✓ LoRA` / `✕ LoRA`** 표시가 있다. LoRA를 쓰려면 `✓`인 모델을 골라야 한다.

### LoRA 관련 항목 (LoRA 지원 모델을 골랐을 때만 나타남)

> **LoRA란?** 특정 화풍·캐릭터·분위기를 학습시킨 "추가 가중치"다. 기본 모델 위에 얹어, 매번 일관된 스타일로 그리게 만든다. 채널 고유의 비주얼을 유지하고 싶을 때 핵심이 되는 기능이다.

- **LoRA URL** — 사용할 LoRA 가중치 파일의 주소(예: fal.ai/Civitai 등에 호스팅된 LoRA 링크)를 넣는다. 비워두면 LoRA 없이 기본 모델로만 생성한다. (X 버튼으로 제거)
- **LoRA Scale (강도)** — LoRA를 **얼마나 강하게** 적용할지(0.1 ~, 기본 1.0). `+ / −` 버튼으로 0.1씩 조절한다.
  - 값이 높을수록 → 학습된 스타일을 강하게 따른다(과하면 부자연스럽거나 프롬프트 내용이 무시될 수 있음).
  - 값이 낮을수록 → 스타일이 옅게 섞인다(원하는 화풍이 약하게 나올 수 있음).
  - 보통 0.7~1.0 사이에서 조절한다.
- **트리거 워드 (Trigger Word)** — 그 LoRA를 "켜는" 키워드다. LoRA는 학습할 때 정해둔 특정 단어가 프롬프트에 들어가야 스타일이 제대로 발동한다. 여기에 입력하면 **모든 이미지 프롬프트 맨 앞에 자동으로 끼워 넣어** 매 컷에서 스타일이 일관되게 적용되도록 한다. (LoRA 제작 시 지정된 단어를 그대로 넣어야 함)
- **스타일 설명 (Style Description)** — 원하는 화풍을 자연어로 적는 칸이다. 예: "어두운 시네마틱, 필름 그레인, 차가운 색감". 이 설명은 **이미지 프롬프트를 만드는 AI(프롬프트 생성 단계)에게 전달**되어, 프롬프트 자체가 그 분위기에 맞는 단어로 쓰이게 한다. 트리거 워드가 "LoRA를 발동시키는 스위치"라면, 스타일 설명은 "프롬프트의 어휘·톤을 그 방향으로 유도하는 가이드"다.

### 세 항목의 역할 한눈에

| 항목 | 무슨 일을 하나 | 적용 시점 |
|------|----------------|-----------|
| LoRA URL | 어떤 학습된 스타일(가중치)을 쓸지 지정 | 이미지 생성 시 모델에 적용 |
| LoRA Scale | 그 스타일을 얼마나 강하게 적용할지 | 이미지 생성 시 |
| 트리거 워드 | LoRA를 발동시키는 키워드를 모든 프롬프트에 자동 삽입 | 이미지 생성 직전 프롬프트 주입 |
| 스타일 설명 | 프롬프트 문장 자체를 원하는 분위기로 쓰도록 AI에 안내 | 이미지 프롬프트 생성 단계 |

> 화면 비율(16:9 / 9:16)도 이미지·영상 생성에 함께 반영된다. 비율은 프로젝트 생성 시 정하지만, 모델·LoRA·스타일은 이미지 생성 전까지 자유롭게 조정하면 된다.

---

## 4. 단계별 진행 가이드 (게이트 조작법)

자동 단계는 기다리면 된다. 아래는 **멈춰서 입력을 기다리는 지점**의 조작법이다. 상태 배지가 **"입력 필요"**로 바뀌면 해당 작업을 하면 된다.

### 게이트 1 — 유튜브 채널 URL (생략 가능)
리서치 후 멈춘다. 벤치마킹할 유튜브 채널 URL이 있으면 입력하고, 없으면 **그냥 다음으로 진행**한다. 입력하면 해당 채널의 훅·제목·톤을 분석해 대본에 반영한다.

### 게이트 2 — 컨셉 선택 (필수)
AI가 **컨셉 3안**(각도·예상 제목 포함)을 제시한다. 마음에 드는 컨셉 하나를 선택한다.
- 3안이 모두 별로면 **"컨셉 재생성"**으로 새 안을 받는다.
- 선택하면 기획 → 대본 → 팩트체크 → 검수가 자동으로 이어진다.

### 게이트 3 — 대본 승인 (조건부)
검수자가 대본에 **100점 만점 점수**를 매긴다.
- **80점 이상 + 필수 수정 없음** → 자동 확정되어 다음 단계로 넘어간다(운영자 조작 불필요).
- **80점 미만 또는 "필수 수정" 항목 있음** → 최대 3회 자동 수정 후, 최종 결과를 운영자가 확인하고 **"승인"**해야 진행된다.
- 검수 의견을 반영해 다시 쓰고 싶으면 **"검토 반영 재작성"**을 사용한다.

### 게이트 4 — 이미지 생성 비용 확인 (필수)
씬·프롬프트가 준비되면 **생성할 이미지 장수와 예상 비용**을 미리 보여준다. 확인 후 진행을 누른다. (이미 생성된 이미지가 있으면 "건너뜀"으로 표시되어 중복 과금되지 않는다.)

### 게이트 5 — 레퍼런스 이미지 업로드 (선택)
화풍·캐릭터를 일관되게 유지하고 싶으면 참고 이미지를 업로드한다. 없으면 건너뛴다. (LoRA/트리거 워드를 쓰는 경우 여기서 반영된다.)

### 게이트 6 — 샘플 이미지 확인 → 전체 생성
비용 낭비를 막기 위해 먼저 **샘플 3장**만 생성한다.
- 결과가 만족스러우면 **전체 이미지 생성**으로 진행.
- 별로면 **"프롬프트 재생성"** 또는 **"이미지 재생성"** 후 다시 확인.

### 게이트 7 — 생성 이미지 최종 확인
전체 이미지가 나오면 검토한다.
- 특정 씬이 마음에 안 들면 해당 씬만 **재생성**.
- 좋으면 **"확인 → 영상 생성"**으로 넘어간다. 이후 영상 클립 생성 → CapCut 빌드까지 자동 진행된다.

---

## 5. 다시 만들기·수정 기능

각 산출물은 부분적으로 다시 만들 수 있다. 비싼 단계(이미지·영상)는 **이미 만든 결과는 건너뛰고 필요한 것만** 다시 생성한다.

| 기능 | 설명 |
|------|------|
| 컨셉 재생성 | 컨셉 선택 대기 중에 새로운 3안을 받는다 |
| 검토 반영 재작성 | 검수 의견을 반영해 대본을 다시 쓴다 |
| 씬 설계 재생성 | 씬 구성을 다시 만든다 |
| 프롬프트 재생성 | 이미지 프롬프트를 다시 만든다 |
| 이미지 재생성 | 이미지를 다시 생성한다(특정 씬 단위 가능) |
| 기존 이미지 재사용 | 이미지 생성 단계를 건너뛰고 기존 이미지를 그대로 사용 |
| TTS 재생성 | 음성을 다시 생성한다 |
| 자막 수정 | 생성된 SRT 자막 문구를 직접 고친다 |
| 캡컷 재생성 | CapCut 프로젝트 파일만 다시 빌드한다 |

---

## 6. 비용 확인

- 각 단계 진행 중 **AI 토큰 비용**, **이미지·영상 생성 비용**이 실시간 로그로 표시된다.
- 이미지·영상은 생성 전 **미리보기 게이트**에서 예상 비용을 먼저 확인할 수 있다.
- 프로젝트 완료 시 `cost-report.json`에 LLM + 이미지 + 영상 합산 비용이 기록된다.

> 참고 단가(모델/시점에 따라 다름): 이미지 약 $0.0025~0.035/장, 영상 약 $0.28/클립.

---

## 7. 완료 후 — CapCut에서 열기

1. 마지막 단계까지 끝나면 상태가 **"완료"**가 된다.
2. **CapCut(캡컷) 앱을 재시작**한다.
3. CapCut 프로젝트 목록에 해당 프로젝트가 자동으로 나타난다. 열어서 최종 편집·자막 조정·내보내기를 진행한다.

생성된 모든 산출물(대본·오디오·자막·이미지·영상)은 프로젝트 폴더(`data/projects/{주제}/`)에 함께 저장되어 있어 필요 시 직접 꺼내 쓸 수 있다.

---

## 8. 오류가 났을 때

- 어떤 단계에서 실패하면 상태가 **"오류"**로 바뀌고 메시지가 표시된다.
- **"시작"(재개)**을 다시 누르면, 이미 완료된 단계는 건너뛰고 **끊긴 지점부터 이어서** 진행한다. (완료한 비싼 작업을 다시 과금하지 않는다.)
- 자주 막히는 원인:
  - AI 단계 즉시 실패 → claude 인증(또는 API 키) 문제
  - 음성·이미지·영상 단계 건너뜀 → 해당 API 키 미설정 (정상 동작이며, 키를 넣으면 활성화)
  - CapCut 단계 실패 → ffmpeg 미설치 또는 CapCut 경로 오류

---

## 9. 팁 & 자주 묻는 질문

- **빠르게 결과만 보고 싶다** → 주제에 "30초"를 넣으면 가장 짧게 만들어진다.
- **컨셉이 다 별로다** → 컨셉 재생성을 2~3번 돌려보고, 그래도 안 맞으면 주제 문구를 더 구체적으로 바꿔 새 프로젝트를 만든다.
- **대본이 자동 확정돼 버렸다** → 80점 이상이면 자동 진행된다. 손보고 싶으면 검수 결과를 보고 "검토 반영 재작성"으로 다시 만든다.
- **이미지 비용이 걱정된다** → 샘플 3장으로 화풍을 먼저 확정한 뒤 전체 생성하면 낭비가 적다.
- **음성이 어색하다** → 홈에서 보이스를 바꾸고 "TTS 재생성"을 사용한다.
- **진행이 멈춘 것 같다** → 로그가 흐르는지 확인. 멈췄다면 "입력 필요" 게이트에서 운영자 동작을 기다리는 중일 수 있다.

---

## 부록 — 상태 배지 의미

| 배지 | 의미 |
|------|------|
| 대기중 | 아직 시작 전 (idle) |
| 실행중 | 자동 단계 진행 중 (running) |
| 입력 필요 | 운영자의 확인·선택을 기다리는 중 (waiting) |
| 완료 | 모든 단계 완료 (completed) |
| 오류 | 단계 실패 — 재개 가능 (error) |
