# VideoMaker 최적화 작업 지시서

> 이 문서는 Claude Code가 읽고 순서대로 수행하기 위한 작업 지시서다.
> 프로젝트 구조·아키텍처는 같은 폴더의 `VideoMaker_프로젝트_분석.md`에 정리돼 있다. **먼저 그 문서를 읽고 시작하라.**

---

## 실행 규칙 (반드시 준수)

- **작업을 한 번에 하나씩** 수행하라. 작업 1 → 2 → 3 → 4 → 5 순서.
- 각 작업이 끝나면 **`npx tsc --noEmit`로 타입 체크가 통과하는지 확인**하고, 변경 파일 목록과 변경 요약을 보고한 뒤 **멈춰서 사용자 승인을 기다려라.** 승인 없이 다음 작업으로 넘어가지 마라.
- 우선순위 1·2만으로도 비용·속도 효과가 크다. 무리하게 한꺼번에 진행하지 마라.

## 공통 제약 (모든 작업에 적용)

- `data/projects/{id}/` 안의 생성 산출물은 **절대 건드리지 마라.**
- `lib/pipeline/claude-runner.ts`의 **SDK/CLI 이중 모드**, 파일 기반 재개(`resumePipeline`), 에러 복구(`lastStatus`) 패턴을 깨지 마라.
- `lib/types.ts`의 `PipelineStatus`와 SSE 이벤트 계약을 임의로 변경하지 마라.
- 각 단계의 **결과물(.md) 내용·형식이 바뀌면 안 된다.** 명시된 작업은 대부분 "구성 재배치/보강" 리팩터링이다.

---

## 작업 1 — 프롬프트 캐싱 적용 (비용 절감, 최우선)

목표: Claude 호출에서 프롬프트 캐싱을 실제로 활용해 입력 토큰 비용을 줄인다.

현재 문제: `lib/pipeline/claude-runner.ts`의 `runClaude()`는 `systemPrompt`와 `cachedPrefix` 인자에 `cache_control: ephemeral` 캐싱을 붙이는 기능이 있지만, 각 단계 모듈(`researcher.ts`, `scriptwriter.ts`, `reviewer.ts`, `script-reviser.ts`, `planner.ts`, `strategist.ts`, `fact-checker.ts`, `scene-designer.ts`, `image-prompter.ts`)은 고정 SYSTEM 프롬프트와 거대한 research/brief/script 컨텍스트를 전부 하나의 `prompt` 문자열로 합쳐서 넘기고 있어 캐시 경로를 전혀 타지 않는다.

해야 할 일:
1. 각 단계 모듈에서 고정 SYSTEM 상수는 `runClaude`의 `systemPrompt` 인자로 분리해서 넘겨라.
2. 호출 간 변하지 않는 큰 컨텍스트(예: `research.md`, `brief.md` 등 그 단계에서 반복 참조되는 입력)는 `cachedPrefix` 인자로 넘기고, 실제로 매번 달라지는 지시문만 `prompt`에 남겨라.
3. 특히 `lib/pipeline/index.ts`의 `runRevisionLoop`는 `reviewer`→`reviser`를 최대 3회 반복하면서 매번 script+brief+fact-check 전체를 재전송한다. 여기서 불변 부분(brief, fact-check, SYSTEM)을 `cachedPrefix`/`systemPrompt`로 빼서 반복 호출이 캐시를 재사용하도록 만들어라.

제약:
- CLI 모드(`runCLI`)는 `cachedPrefix`를 단순 문자열로 이어 붙이는 기존 동작을 유지하면 된다(캐싱은 SDK 모드 전용).
- 출력 .md 형식과 각 단계 결과물 내용이 바뀌면 안 된다. 프롬프트 "구성"만 재배치하는 리팩터링이다.
- 완료 후 `npx tsc --noEmit` 통과 확인.

---

## 작업 2 — 독립 단계 병렬화 (대기시간 단축)

목표: 서로 입력 의존이 없는 단계를 병렬 실행해 체감 대기시간을 줄인다.

대상 1 — `lib/pipeline/index.ts`의 `runPostScript()`:
현재 TTS → 씬 설계 → 이미지 프롬프트를 순차 실행한다. 하지만 TTS(`runTTS`, ElevenLabs)와 씬 설계(`runSceneDesigner`)는 둘 다 script만 입력으로 쓰고 서로 의존하지 않는다. `runTTS`와 (`runSceneDesigner` → `runImagePrompter`) 체인을 `Promise.all`로 동시에 실행하도록 바꿔라. 단, 상태(`updateStatus`)와 SSE `emit` 순서가 꼬이지 않도록 주의하고, 둘 중 하나가 실패하면 기존처럼 `handleError`로 전체 에러 처리되게 유지하라. 최종적으로 `waiting:cost-images` 게이트로 가는 흐름은 동일해야 한다.

대상 2 — `lib/pipeline/researcher.ts`:
`queries` 4개를 for-await로 순차 검색하는 부분을 `Promise.all`로 병렬화하라. 결과 순서와 로그 출력은 유지하고, `tavilySearch`가 null을 반환하는 경우(키 없음/실패) 기존처럼 건너뛰는 동작을 보존하라.

제약:
- 파일 기반 재개 로직이 깨지지 않아야 한다(각 산출물이 정상 기록되는지 확인).
- 완료 후 `npx tsc --noEmit` 통과 확인.

---

## 작업 3 — LLM 출력 파싱 견고화 (안정성)

목표: 정규식으로 LLM 마크다운 출력을 긁어오는 취약한 파싱을 구조화 출력으로 보강한다.

현재 문제: `lib/project.ts`의 `parseReviewScore`(점수/판정)와 `parseConcepts`(컨셉 추출)가 LLM이 마크다운 형식을 정확히 지킨다는 전제에 의존한다. 형식이 흔들리면 점수가 0으로 잡히거나 컨셉이 비는 버그가 난다.

해야 할 일:
1. `reviewer.ts`의 시스템 프롬프트에 "마지막에 반드시 다음 형식의 JSON 코드블록을 출력하라"는 지시를 추가하라:
   `{"score": 0-100 정수, "verdict": "합격|조건부 합격|불합격"}`
2. `parseReviewScore`를 수정해 먼저 이 JSON 블록을 파싱하고, 없거나 깨졌으면 **기존 정규식 방식으로 폴백**하라. (기존 정규식 로직은 절대 삭제하지 말고 폴백으로 남겨라.)
3. `strategist.ts`/`parseConcepts`도 같은 패턴으로, 컨셉 목록을 JSON으로도 함께 출력하게 하고 `parseConcepts`가 JSON 우선 → 정규식 폴백 순으로 동작하게 하라.

제약:
- 기존에 생성된 `strategy.md` / `script-review.md` 파일도 폴백 정규식으로 여전히 읽혀야 한다(하위 호환).
- `score`/`verdict`, `Concept` 타입 시그니처는 바꾸지 마라.
- 완료 후 `npx tsc --noEmit` 통과 확인.

---

## 작업 4 — 환경 의존성 및 사소한 버그 정리 (난이도 낮음)

목표: 환경 비의존성을 높이고 명백한 사소 버그를 정리한다.

1. `lib/pipeline/claude-runner.ts`: `CLAUDE_BIN` 기본값이 `/Users/hongss/.local/bin/claude`로 하드코딩돼 있다. `process.env.CLAUDE_BIN`가 없으면 `which claude`(실패 시 PATH의 `claude`)로 해석하도록 폴백을 추가하라. 기존 환경변수 우선순위는 유지.
2. `lib/pipeline/video-generator.ts`의 클립 수집 루프(약 205행)에서 `const imageFile = i < sceneImages.length ? sceneImages[i] : sceneImages[sceneImages.length - 1];`는 루프 조건상 항상 참이라 죽은 코드다. `sceneImages[i]`로 단순화하라.
3. `lib/s3.ts`의 `console.error` 호출들을 검토해, 실패를 조용히 무시하지 않되 빌드 규칙에 맞는 형태로 정리하라(간단한 로깅 래퍼 또는 의도 명시 주석).

제약: 동작 변화 없이 정리만. 완료 후 `npx tsc --noEmit` 및 `npm run build` 통과 확인.

---

## 작업 5 — 핵심 로직 단위 테스트 추가 (선택, 안전망)

목표: 가장 취약한 순수 함수에 단위 테스트를 붙여 회귀를 막는다.

대상: `lib/project.ts`의 `parseConcepts`, `parseReviewScore`, `slugify`와 `lib/pipeline/index.ts`의 `hasMandatoryRevisions`.

해야 할 일:
1. 이 저장소에 맞는 경량 테스트 러너를 설정하라(Vitest 권장, 없으면 `node:test`도 가능). devDependency로만 추가.
2. 실제 `data/projects` 안의 기존 `strategy.md` / `script-review.md` 형식을 참고해, 정상 케이스와 형식이 깨진 케이스(폴백 동작)를 모두 커버하는 테스트를 작성하라.
3. `package.json`에 `"test"` 스크립트를 추가하라.

제약:
- 프로덕션 코드 로직은 바꾸지 말고 테스트만 추가하라(작업 3을 먼저 적용했다면 JSON 우선 케이스도 포함).
- 완료 후 `npm test`와 `npx tsc --noEmit` 통과 확인.

---

## 권장 진행 순서

**1 → 2 → 3 → 4 → 5.** 작업 1·2만으로도 비용·속도 효과는 즉시 체감된다. 각 작업 후 `tsc`/`build` 결과를 보고하고 사용자 승인을 받은 뒤 다음으로 진행하라.
